spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/StatementAccounts
    username: postgres
    password: "*********"
    test-connection: true
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true